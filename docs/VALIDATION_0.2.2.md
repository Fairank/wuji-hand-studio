# Windows 0.2.2 验证记录

## 已检查

| 场景 | 结果 |
|---|---|
| 动作页滚动 | 1280×720 浏览器复测：正文独立滚动约 610 px，窗口滚动为 0；顶栏位于 17–77 px，阅读区从 90 px 开始，标题不会穿过顶栏。 |
| 窄窗口 | 850×720 无横向溢出；连接页分区按钮可用。 |
| 分区与跳转 | 设备、实时可视化、手套校准、手套→手映射均可切换；旧映射控件和事件监听保留，手套入口跳至新分区。 |
| 无设备空态 | 无手套时 21 点骨架收起并标记“未接收数据”；未配置官方控制端时开始标定禁用。 |
| 官方 CLI 标定边界 | 命名用户、已有模型显式覆盖、最新扫描手套、SSH 只读状态由单元测试覆盖。新流程不启动机械手电机。 |
| 程序测试 | `python -m unittest discover -s src`：378 项，377 通过、1 跳过。`desktop.py --self-check --render-check` 成功，20 关节、四种手型、58 项动作、离线渲染成功。 |
| Windows 程序 | PyInstaller 构建及其自检通过；隔离数据目录的打包程序启动服务后，状态接口与新前端资源均返回成功。 |
| 安装包 | 当前用户静默原位更新退出码 0；系统卸载项只见 Hand Workbench 0.2.2；安装目录中新界面文件 SHA-256 与源码完全一致。 |
| Claude 辅助 | 仅公开双语界面文案；CLI 请求 `claude-opus-5-5 --effort max`，实际返回模型 `claude-opus-5-5`，完成状态 success。主维护者审核后采用。 |

## 尚未完成

- 未接入真实手套跑六姿势标定，因此没有实机标定成功率、手套骨架延迟或取消流程的硬件验收。
- 外部背景折射在 Windows 原生窗口里仍为实验功能，需用户在“设置 → 外观”主动开启；浏览器截图只能验证边缘不再有遮挡条，不能证明桌面图案折射质量。
- macOS / Ubuntu 本轮共用前端源码，未构建新安装包或在这些系统上验证本轮界面。
- 真实机械手动作、控制增益、轨迹和连接协议未在本轮改动；未做实机动作测试。

公开源码与安装包不含私有模型、用户数据、设备记录和凭据。正式安装包 `HandWorkbench-0.2.2-windows-x64-setup.exe` 的 SHA-256 为 `45889ebf79a05850670b7b28e17d8f7d21f5a6e8e313075558ffbda3481c0217`，另见同名 `.sha256` 文件。
