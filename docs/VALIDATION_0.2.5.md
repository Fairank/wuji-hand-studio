# Windows 0.2.5 折射持续开启验证记录

按用户要求，性能警告不再关闭折射。当前仅改变外观采集和显示逻辑，不改变机械手连接或动作。

## 已核验

- 380 项 Python 测试通过，1 项按原条件跳过；独立 JavaScript 刷新诊断测试通过。
- 浏览器合成场景：45 毫秒主线程停顿后报告约 48–61 毫秒，折射开关仍选中、画布仍存在；30 Hz 连续画面后仍保持开启；静止 750 毫秒后仍保持开启；手动关闭后画布移除、说明改为“折射已关闭”。无控制台错误。合成画面不包含真实桌面像素。
- 最小化时原生端不因无请求而结束会话；网页对非活动帧保留最新序号，使下次原生读取等待新帧，避免旧序号导致的忙等。此项依据代码路径和逻辑核查，未在真实窗口最小化场景测量 CPU。
- Windows 0.2.5 安装器 SHA-256：`559c7b4429fa56044d435401edc9cee1e647ec8d75cec6250cd642940a15c3bb`。已完成安装、安装后离线自检和桌面检查；版本显示 0.2.5、界面错误列表为空，机械手断开，折射按默认设置关闭。用户开启折射后才采集外部桌面边缘。

## 尚未通过的现场验收

- 真实动态桌面的连续帧率、窗口最小化后自动恢复及显卡上下文中断恢复尚未进行现场试验；不能把合成结果说成真实桌面 60–120 Hz 验收。
