# 0.2.7 原生磨砂与设置

本版按用户决定移除实验折射。旧捕获、JPEG传输、WebGL折射、设置控件、桥接入口及专用依赖已移除；保留系统原生磨砂。历史版本报告仍保留，不代表当前能力。

## 已核对

- Python 全套375项，374通过、1跳过。移除的7项只覆盖已删除的折射实现。
- 5项连接流程检查通过；本轮没有连接硬件。
- 修改的前端脚本语法检查通过。
- 独立数据目录运行真实本地服务，浏览器检查设置三分区、手套映射跳转、白/黑主题、中英文、减少动态效果；无脚本错误、无横向溢出、无折射画布或设置节点。
- 持续反馈刷新期间，节目单1.75×值及焦点保留。修改整单随机顺序、3轮循环，重新加载后保留。测试发现数字输入只监听change时不能可靠保存，已同时监听input并复查。
- 设置复用原控件/事件；本版不更改电机参数、硬件轨迹或已保存的设备配置。

Windows可执行文件构建与内置自检通过，安装器构建成功，通过工作台自身维护接口原位更新。同一应用已重新打开，版本0.2.7，原生材质`desktop_acrylic`，桌面捕获为false，设置页无脚本错误、无横向溢出。旧四个脚本文件已删除；一个被旧进程占用的采集模块在旧进程退出后清理完成。

首次安装启动曾出现页面初始化不全、维护导航报`showPage`不存在。保留日志；通过空闲UI重新加载恢复，随后正常退出再冷启动，动作页和设置页均正常、错误列表为空。尚未定位首次异常根因，不把一次恢复声称为所有冷启动故障已根治。

macOS仅调整源码到原生vibrancy，未在Mac运行。本版没有以软件检查替代手、手套、多手或私人模型实机验收。安装包SHA-256：`02b17be6e595ddced960e10f01562828ea7abf63ad28a2e057b867a02e6c7f28`。
